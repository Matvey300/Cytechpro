# ===================================================
# 🔧 Project Configuration — Amazon Review Intelligence Tool
# ===================================================
# This file contains all project-level environment variables.
# Always verify that all variables are actually used in the codebase.
# Owner: Matvey Bosin

# API keys provided only for quick start / testing purposes
SERPAPI_API_KEY="b706e168b0b09f00eda605764e7acbd4b4156153ffbf22ca43a80e417fcaddb2"
# API key for accessing SerpAPI (category search)
CHROME_USER_DATA_DIR="./DATA/.chrome_profile"
# Path to Chrome user data directory for Selenium sessions
CHROME_PROFILE_DIR="Default"
# Specific Chrome profile to use (e.g., 'Default')
SCRAPINGDOG_API_KEY="6888b22c09c987d9f10c066e"
# API key for Scrapingdog (ASIN & reviews HTML parsing)

# Browser visibility mode: normal | minimize | offscreen | headless
BROWSER_VISIBILITY="offscreen"
# Choose how browser window behaves: normal | minimize | offscreen | headless

# Amazon account credentials (optional, for automated login)
AMAZON_EMAIL="matvey300@yahoo.com"
# Amazon login email for automated scraping session
AMAZON_PASSWORD="your_password_here"
# Amazon password (keep secure or load from secret manager)

# --- Amazon marketplace and crawl limits ---
DEFAULT_MARKETPLACE="com"
# Default Amazon domain suffix (e.g., 'com', 'co.uk', 'de')
REVIEWS_MAX_PAGES=30
# Maximum number of review pages to scrape per ASIN
REVIEWS_MAX_PER_ASIN=200
# Cap on total reviews per ASIN
SAVE_RAW_HTML=1
# 1 = save HTML of review pages to /Raw/reviews/ ; 0 = skip saving
SAVE_PRODUCT_PNG=1
# 1 = save product preview screenshots ; 0 = skip screenshots

# Enable cart-based price resolution when price is hidden on DP/reviews page
ENABLE_CART_PRICE=1
# 1 = attempt to add to cart and read price from cart when price is hidden

# --- Optional external providers (set to 1 to disable) ---
DISABLE_SCRAPINGDOG=0
DISABLE_SERPAPI=0

# Crawl behavior
# When enabled (1), do not stop by date if the page still contains previously unseen review_id
CONTINUE_IF_NEW_IDS=1
