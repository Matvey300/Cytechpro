{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "\\.(py|ipynb|csv)$",
        "cmd": "git add *.py *.ipynb && git commit -m \"Auto commit on save\" && git push"
      }
    ]
  }
}
